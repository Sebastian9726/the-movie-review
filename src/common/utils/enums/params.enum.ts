
export enum ResponseMessage {
  'RR-01' = 'The review has been successfully created.',

}

export enum ErrorMessage {
  'UE-01' = 'Doesn´t find the user ',

}

export enum LogMethod {
  'log' = 'log',
  'debug' = 'debug',
  'error' = 'error',
  'warn' = 'warn',
}
